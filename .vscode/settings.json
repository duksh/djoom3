{
    "docker.certPath": "",
    "sonarlint.connectedMode.project": {
        "connectionId": "${env:SONARLINT_CONNECTION_ID}",
        "projectKey": "${env:SONARLINT_PROJECT_KEY}"
    },
    "docker.environment": {
        "DOCKER_HOST": "tcp://localhost:2375",
        "DOCKER_TLS_VERIFY": "0"
    }
    
}
